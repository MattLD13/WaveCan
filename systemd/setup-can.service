[Unit]
Description=Setup CAN Interface
After=network.target

[Service]
Type=oneshot
ExecStart=/bin/sh -c "/bin/ip link set can1 down 2>/dev/null || true; /bin/ip link set can1 up type can bitrate 1000000"
ExecStop=/bin/ip link set can1 down
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
